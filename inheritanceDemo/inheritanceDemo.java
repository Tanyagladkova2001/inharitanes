/*package com.figures.inheritanceDemo;

import com.figures.figures.*;

public class inheritanceDemo {
        public static void main(String[] args) {
            Shape shape = new Shape(1.5);
            Pyramid pyramid = new Pyramid(2.0, 2.5);
            Cylinder cylinder = new Cylinder(3.0);
            Ball ball = new Ball();
            SolidOfRevolution solidOfRevolution = new SolidOfRevolution(1.7);

           // shape.setVolume(new Shape(1.5));
            //cylinder.setRadius(new Cylinder(2.0));
          //  Shape.setS(new Pyramid(3.5, 2.5));
          // ball.setRadius(new SolidOfRevolution(5.5));

            shape.shape();
            cylinder.shape();
            solidOfRevolution.shape();
            ball.shape();



        }
    } */

